
// Generates a random number between 10 and 100 in float
const rnum = Math.random() * (100 - 10) + 10;
// Rounds off to the nearest integer
const inum = Math.floor(rnum);
// Prints the value to console
console.log(inum);